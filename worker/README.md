# Worker

Cloudflare Workers API. Routes defined in index.ts.

## Structure

- `/src/index.ts` — Main worker entry point and route handler

## Development

Run with wrangler from the project root:

```bash
npx wrangler dev
```

## Deployment

```bash
npx wrangler deploy
```

## Configuration

Worker settings are defined in `wrangler.toml` at the project root.
