# Frontend

Svelte + Vite frontend. Builds to /dist for Pages deployment.

## Structure

- `/src` — Svelte components and TypeScript source
- `/public` — Static assets (copied to build output)

## Development

```bash
npm run dev
```

## Build

```bash
npm run build
```

Output goes to `../dist` for Cloudflare Pages deployment.
